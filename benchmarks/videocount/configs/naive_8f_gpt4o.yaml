name: naive_8f_gpt4o
description: "Naive single-shot baseline: 8 uniform frames, GPT-4o"

mode: naive
vlm_model: gpt-4o
n_frames: 8

dataset:
  split: val
  video_cache_dir: /tmp/rvlm_videos

output:
  results_dir: benchmarks/videocount/results
