name: naive_16f_gemini_25_flash
description: "Naive single-shot baseline: 16 uniform frames, Gemini 2.5 Flash"

mode: naive
vlm_model: gemini-2.5-flash
n_frames: 16

dataset:
  split: val
  video_cache_dir: /tmp/rvlm_videos

output:
  results_dir: benchmarks/videocount/results
