name: naive_8f_gemini_25_flash
description: "Naive single-shot baseline: 8 uniform frames, Gemini 2.5 Flash"

mode: naive
vlm_model: gemini-2.5-flash
n_frames: 8

dataset:
  split: val
  video_cache_dir: /tmp/rvlm_videos

output:
  results_dir: benchmarks/videocount/results
