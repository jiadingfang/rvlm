name: naive_16f_claude
description: "Naive single-shot baseline: 16 uniform frames, Claude Sonnet 4.5"

mode: naive
vlm_model: claude-sonnet-4-5
n_frames: 16

dataset:
  split: val
  video_cache_dir: /tmp/rvlm_videos

output:
  results_dir: benchmarks/videocount/results
